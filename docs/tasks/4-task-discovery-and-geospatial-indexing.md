# Tasks - Discovery and Geospatial Indexing (Phase 4)

- [ ] Database Migration
    - [ ] Create `migrations/202605050003_create_partners_table.sql`
    - [ ] Define `partners` table with `id` (UUID PK), `user_id` (FK → users), `service_type`, `is_active`, `rating`, `created_at`
    - [ ] Add `-- +goose Up` and `-- +goose Down` directives
- [ ] Feature Slice Scaffold
    - [ ] Create `internal/features/discovery/` directory structure
        - [ ] `delivery/`   – HTTP handlers
        - [ ] `repository/` – Redis GEO commands
        - [ ] `usecase/`    – Distance logic & filtering
        - [ ] `dto/`        – `LocationRequest`, `NearbyPartnerResponse`
- [ ] DTO Layer
    - [ ] Define `LocationRequest` struct (`Latitude float64`, `Longitude float64`)
    - [ ] Define `NearbyPartnerResponse` struct (partner ID, distance, profile fields)
- [ ] Repository Layer
    - [ ] Create `internal/features/discovery/repository/` implementation file
    - [ ] Implement `UpdateLocation` using `GEOADD partner:locations <lon> <lat> <partner_id>`
    - [ ] Implement `GetNearby` using `GEOSEARCH` with `WITHDIST` and `Sort: "asc"`
    - [ ] Use key pattern `partner:locations` (no TTL)
- [ ] Use Case Layer
    - [ ] Create use case interface and implementation
    - [ ] Implement coordinate validation (Latitude −90…90, Longitude −180…180)
    - [ ] Implement distance formatting (e.g., "1.2 km")
    - [ ] (Optional) Enrich results by fetching partner profile details from PostgreSQL
- [ ] Delivery Layer
    - [ ] Create `internal/features/discovery/delivery/` HTTP handler file
    - [ ] Implement `POST /v1/discovery/location` handler
        - [ ] Parse `LocationRequest` from request body
        - [ ] Extract `partner_id` from session (Phase 2 context)
        - [ ] Call use case `UpdateLocation`
    - [ ] Implement `GET /v1/discovery/nearby` handler
        - [ ] Parse query params: `lat`, `lon`, `radius`, `unit`
        - [ ] Call use case `GetNearby`
        - [ ] Return list with distance field per entry
- [ ] Router Registration
    - [ ] Register discovery routes under `/v1/discovery` in `cmd/api/main.go`
- [ ] Verification (Definition of Done)
    - [ ] `POST /v1/discovery/location` correctly writes to Redis (verify with `GEOPOS partner:locations <id>`).
    - [ ] `GET /v1/discovery/nearby` returns partner IDs within the specified radius.
    - [ ] Response includes distance per partner (e.g., "1.2 km").
    - [ ] Invalid coordinates return a proper error response (400 Bad Request).
    - [ ] Code follows Feature Slice architecture and is compatible with Go v1.26.2.
